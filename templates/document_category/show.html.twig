{% extends 'base.html.twig' %}

{% block title %}DocumentCategory{% endblock %}

{% block body %}
    <h1>DocumentCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ document_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_document_category_index') }}">back to list</a>

    <a href="{{ path('app_document_category_edit', {'id': document_category.id}) }}">edit</a>

    {{ include('document_category/_delete_form.html.twig') }}
{% endblock %}
